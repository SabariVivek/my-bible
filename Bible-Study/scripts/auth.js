/**
 * AUTH.JS - Authentication handling for Bible Study application
 * Login functionality has been completely removed
 * App now displays dashboard directly without authentication
 */

// No authentication checks - app loads dashboard directly
document.addEventListener('DOMContentLoaded', function() {
    // All login page functionality has been disabled
});

// Logout function - disabled
function logout() {
    console.log('Logout feature has been disabled - app no longer uses authentication');
}
